{
  "name": "torch-js",
  "version": "0.2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Kittipat Virochsiri, arition",
  "license": "MIT",
  "scripts": {
    "install": "yarn compile",
    "build": "rm -r dist/ && tsc && cp lib/torch.* dist/",
    "compile": "cmake-js compile",
    "rebuild": "cmake-js rebuild"
  },
  "devDependencies": {
    "@types/node": "^14.6.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "cmake-js": "^6.1.0",
    "node-addon-api": "^3.0.0"
  },
  "binary": {
    "napi_versions": [
      3
    ]
  }
}
